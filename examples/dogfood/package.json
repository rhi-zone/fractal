{
  "name": "@rhi-zone/fractal-example-dogfood",
  "version": "0.0.0",
  "private": true,
  "description": "A real-shaped auth+validation feature slice ported to fractal, to validate the framework against reality",
  "type": "module",
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test",
    "codegen": "bun ../../packages/codegen/src/cli.ts ./src/app.ts --out ./src/generated --title 'Prospects API' --version '1.0.0'"
  },
  "dependencies": {
    "@rhi-zone/fractal-client": "workspace:*",
    "@rhi-zone/fractal-core": "workspace:*",
    "@rhi-zone/fractal-http": "workspace:*"
  },
  "devDependencies": {
    "@rhi-zone/fractal-codegen": "workspace:*"
  }
}
