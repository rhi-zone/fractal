{
  "name": "@rhi-zone/fractal-example-todo-api",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end example: Bun server + fetch client transport",
  "type": "module",
  "scripts": {
    "typecheck":  "tsgo --noEmit",
    "test":       "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@rhi-zone/fractal-core":   "workspace:*",
    "@rhi-zone/fractal-facade": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
