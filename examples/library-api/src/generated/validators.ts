// @generated by @rhi-zone/fractal-api-tree — do not edit
// AUTO-GENERATED by @rhi-zone/fractal-type-ir. Do not edit by hand.

export const validators = {
  "books/list": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          true
        )
      }
    })() as (value: unknown) => value is unknown;
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: books/list" } });
  })(),
  "books/add": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['title'] === 'string') &&
          (typeof value['author'] === 'string') &&
          (typeof value['genre'] === 'string')
        )
      }
    })() as (value: unknown) => value is { title: string; author: string; genre: string };
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: books/add" } });
  })(),
  "books/:bookId/read": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string')
        )
      }
    })() as (value: unknown) => value is { bookId: string };
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: books/:bookId/read" } });
  })(),
  "books/:bookId/replace": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string') &&
          (value['title'] !== undefined ? ((typeof value['title'] === 'string')) : true) &&
          (value['author'] !== undefined ? ((typeof value['author'] === 'string')) : true) &&
          (value['genre'] !== undefined ? ((typeof value['genre'] === 'string')) : true)
        )
      }
    })() as (value: unknown) => value is { bookId: string; title?: string; author?: string; genre?: string };
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: books/:bookId/replace" } });
  })(),
  "books/:bookId/remove": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string')
        )
      }
    })() as (value: unknown) => value is { bookId: string };
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: books/:bookId/remove" } });
  })(),
  "books/:bookId/checkout/start": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string')
        )
      }
    })() as (value: unknown) => value is { bookId: string };
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: books/:bookId/checkout/start" } });
  })(),
  "books/:bookId/checkout/reserve": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string') &&
          (typeof value['patronId'] === 'string')
        )
      }
    })() as (value: unknown) => value is { bookId: string; patronId: string };
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: books/:bookId/checkout/reserve" } });
  })(),
  "catalog/search": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (value['q'] !== undefined ? ((typeof value['q'] === 'string')) : true)
        )
      }
    })() as (value: unknown) => value is { q?: string };
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: catalog/search" } });
  })(),
  "catalog/genres": (function () {
    const check = (function () {
      return function check(value: any) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (value['prefix'] !== undefined ? ((typeof value['prefix'] === 'string')) : true)
        )
      }
    })() as (value: unknown) => value is { prefix?: string };
    return (bag: Record<string, unknown>) => (check(bag) ? { kind: "ok" as const, value: bag } : { kind: "err" as const, error: { message: "validation failed: catalog/genres" } });
  })(),
}
