// @generated by @rhi-zone/fractal-api-tree — do not edit
// @ts-nocheck
// AUTO-GENERATED by @rhi-zone/fractal-type-ir. Do not edit by hand.

export const validators = {
  "books/list": (function () {
    const check = (function () {
      return function check(value) {
        return (
          true
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: books/list" } });
  })(),
  "books/add": (function () {
    const check = (function () {
      return function check(value) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['title'] === 'string') &&
          (typeof value['author'] === 'string') &&
          (typeof value['genre'] === 'string')
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: books/add" } });
  })(),
  "books/:bookId/read": (function () {
    const check = (function () {
      return function check(value) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string')
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: books/:bookId/read" } });
  })(),
  "books/:bookId/replace": (function () {
    const check = (function () {
      return function check(value) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string') &&
          (value['title'] !== undefined ? ((typeof value['title'] === 'string')) : true) &&
          (value['author'] !== undefined ? ((typeof value['author'] === 'string')) : true) &&
          (value['genre'] !== undefined ? ((typeof value['genre'] === 'string')) : true)
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: books/:bookId/replace" } });
  })(),
  "books/:bookId/remove": (function () {
    const check = (function () {
      return function check(value) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string')
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: books/:bookId/remove" } });
  })(),
  "books/:bookId/checkout/start": (function () {
    const check = (function () {
      return function check(value) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string')
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: books/:bookId/checkout/start" } });
  })(),
  "books/:bookId/checkout/reserve": (function () {
    const check = (function () {
      return function check(value) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (typeof value['bookId'] === 'string') &&
          (typeof value['patronId'] === 'string')
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: books/:bookId/checkout/reserve" } });
  })(),
  "catalog/search": (function () {
    const check = (function () {
      return function check(value) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (value['q'] !== undefined ? ((typeof value['q'] === 'string')) : true)
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: catalog/search" } });
  })(),
  "catalog/genres": (function () {
    const check = (function () {
      return function check(value) {
        return (
          (typeof value === 'object' && value !== null && !Array.isArray(value)) &&
          (value['prefix'] !== undefined ? ((typeof value['prefix'] === 'string')) : true)
        )
      }
    })();
    return (bag) => (check(bag) ? { kind: "ok", value: bag } : { kind: "err", error: { message: "validation failed: catalog/genres" } });
  })(),
}
