{
  "name": "@rhi-zone/fractal-channel-websocket",
  "version": "0.1.0-alpha.0",
  "description": "WebSocket channel axis instance (pure Channel + Bun server factory) for the fractal transport",
  "type": "module",
  "main": "./dist/channel-websocket.umd.cjs",
  "module": "./dist/channel-websocket.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types":   "./dist/index.d.ts",
      "import":  "./dist/channel-websocket.js",
      "require": "./dist/channel-websocket.umd.cjs"
    }
  },
  "files": ["dist"],
  "scripts": {
    "dev":        "vite build --watch",
    "build":      "vite build && tsgo --emitDeclarationOnly",
    "typecheck":  "tsgo --noEmit",
    "test":       "bun test",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@rhi-zone/fractal-transport":             "workspace:*"
  },
  "devDependencies": {
    "@rhi-zone/fractal-core":                  "workspace:*",
    "@rhi-zone/fractal-codec-json":            "workspace:*",
    "@rhi-zone/fractal-protocol-correlation":  "workspace:*",
    "@rhi-zone/fractal-preset-websocket":      "workspace:*",
    "fast-check": "^4.5.3",
    "vite":       "^6.0.0",
    "vitest":     "^2.0.0"
  }
}
