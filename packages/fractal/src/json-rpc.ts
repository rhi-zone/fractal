// packages/fractal/src/json-rpc.ts — @rhi-zone/fractal/json-rpc
//
// Re-exports `@rhi-zone/fractal-json-rpc-api-projector`'s root surface. The
// wire-format types and standard error codes live on that package's `./wire`
// subpath but are already re-exported from its root, so they arrive here too.

export * from "@rhi-zone/fractal-json-rpc-api-projector";
