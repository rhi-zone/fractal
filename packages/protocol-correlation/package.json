{
  "name": "@rhi-zone/fractal-protocol-correlation",
  "version": "0.1.0-alpha.0",
  "description": "Duplex multiplexed correlation protocol axis instance for the fractal transport",
  "type": "module",
  "main": "./dist/protocol-correlation.umd.cjs",
  "module": "./dist/protocol-correlation.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types":   "./dist/index.d.ts",
      "import":  "./dist/protocol-correlation.js",
      "require": "./dist/protocol-correlation.umd.cjs"
    }
  },
  "files": ["dist"],
  "scripts": {
    "dev":        "vite build --watch",
    "build":      "vite build && tsgo --emitDeclarationOnly",
    "typecheck":  "tsgo --noEmit",
    "test":       "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@rhi-zone/fractal-core":      "workspace:*",
    "@rhi-zone/fractal-transport": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.5.3",
    "vite":       "^6.0.0",
    "vitest":     "^2.0.0"
  }
}
