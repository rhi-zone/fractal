{
  "name": "@rhi-zone/fractal-core",
  "version": "0.1.0-alpha.0",
  "description": "Agnostic Handler core — Pass, Req<P>, Handler<P,Res>, choice, pipe, capture, typed, leaf, run",
  "type": "module",
  "main": "./dist/core.umd.cjs",
  "module": "./dist/core.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types":   "./dist/index.d.ts",
      "import":  "./dist/core.js",
      "require": "./dist/core.umd.cjs"
    }
  },
  "files": ["dist"],
  "scripts": {
    "dev":        "vite build --watch",
    "build":      "vite build && tsgo --emitDeclarationOnly",
    "typecheck":  "tsgo --noEmit",
    "test":       "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite":    "^6.0.0",
    "vitest":  "^2.0.0"
  }
}
