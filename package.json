{
  "name": "fractal-monorepo",
  "private": true,
  "type": "module",
  "comment": "Workspaces fenced to the function-core spine slice. packages/{openapi,client} and examples/{todo-api,dogfood} are FENCED OUT pending migration to the function-core model — see TODO.md. packages/codegen was migrated (types+JSDoc → runtime schema extractor).",
  "workspaces": [
    "packages/core",
    "packages/http",
    "packages/mcp",
    "packages/codegen",
    "packages/openapi",
    "examples/library-api"
  ],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "@typescript/native-preview": "latest",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
