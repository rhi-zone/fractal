{
  "name": "fractal-monorepo",
  "private": true,
  "type": "module",
  "comment": "Workspaces fenced to the function-core spine slice. packages/{openapi,codegen,client} and examples/{todo-api,dogfood} are FENCED OUT pending migration to the function-core model — see TODO.md.",
  "workspaces": [
    "packages/core",
    "packages/http",
    "examples/spine-demo"
  ],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "@typescript/native-preview": "latest",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
